<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Habit" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Habit.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="postSplashScreenTheme">@style/Theme.Habit</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_screen</item>
    </style>

</resources>